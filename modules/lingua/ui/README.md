# Legacy UI (Deprecated)

> **⚠️ AVISO ESTRUCTURAL IMPORTANTE:**
> Esta interfaz de usuario (HTML/JS de Lingua) bajo `modules/` es **Legacy** y se mantiene únicamente por compatibilidad de rutas antiguas o de backend que puedan invocarla temporalmente en desarrollo.
>
> **Bajo la Arquitectura de Chips v0.2.0, la UI oficial vive en `chips/chip-idiomas-ia/frontend/`.**

Todos los nuevos features, integraciones y mejoras visuales deben construirse e implementarse en `chip-idiomas-ia`. El orquestador FastApi seguirá sirviendo esto por el momento, pero el punto de entrada principal hacia el usuario (Dashboard) prioriza la experiencia del nuevo chip.
