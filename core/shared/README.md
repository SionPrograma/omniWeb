# Shared Core Resources
Recursos compartidos por todos los chips de OmniWeb.

- `components/`: Componentes UI reutilizables (navbar, modales, etc.).
- `styles/`: Estilos base, tokens de diseño y utilidades CSS.
- `utils/`: Helpers de JavaScript (formatters, validators, etc.).
